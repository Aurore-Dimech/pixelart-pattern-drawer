# v1.plan.md — Plan contractuel PixelArt App

**Statut** : 🔄 À valider par l'humain avant exécution  
**Date de création** : phase initiale  
**Périmètre** : MVP fonctionnel couvrant auth + éditeur + sauvegarde + galerie + favoris

> ⚠️ Ce fichier est un **contrat**. Aucune tâche ne doit être implémentée sans validation humaine préalable.  
> Une fois validé, cocher les cases au fur et à mesure de l'avancement.

---

## Objectif de la v1

Livrer une application web fonctionnelle où un utilisateur peut :
1. Créer un compte et se connecter
2. Créer un dessin pixel art dans un éditeur interactif
3. Sauvegarder ses dessins
4. Publier un dessin dans la galerie publique
5. Consulter la galerie et ajouter des dessins en favoris

---

## Périmètre inclus (IN SCOPE)

| # | Fonctionnalité | Priorité |
|---|---|---|
| F1 | Inscription / Connexion (email + password) | 🔴 Critique |
| F2 | Éditeur pixel art (grille 16×16 par défaut, palette, crayon, gomme) | 🔴 Critique |
| F3 | Sauvegarde d'un dessin (titre + gridData) | 🔴 Critique |
| F4 | Dashboard "Mes dessins" (liste, édition, suppression) | 🔴 Critique |
| F5 | Publication dans la galerie publique | 🟠 Important |
| F6 | Galerie publique (grille de dessins publiés) | 🟠 Important |
| F7 | Ajout / retrait de favoris | 🟠 Important |
| F8 | Page "Mes favoris" | 🟡 Nice-to-have |
| F9 | Export PNG du dessin | 🟡 Nice-to-have |

## Périmètre exclu (OUT OF SCOPE v1)

- Suggestion de palette par IA (v2)
- Commentaires sur les dessins
- Tags / catégories
- Profil public utilisateur
- Système de likes / votes
- Animations pixel art

---

## Tâches détaillées

### Étape 1 — Infrastructure (prerequis)

- [ ] **T1.1** Initialiser le projet Next.js avec TypeScript + Tailwind + ESLint
- [ ] **T1.2** Configurer Prisma avec SQLite
- [ ] **T1.3** Définir le schéma Prisma (User, Drawing, Favorite, Session, Account)
- [ ] **T1.4** Exécuter la première migration `npx prisma migrate dev --name init`
- [ ] **T1.5** Créer `src/lib/prisma.ts` (singleton client)
- [ ] **T1.6** Créer `src/types/index.ts` (types partagés)

**Gate T1** : `npx prisma studio` s'ouvre sans erreur, les 3 tables sont visibles.

---

### Étape 2 — Authentification

- [ ] **T2.1** Installer et configurer NextAuth.js v5 avec Prisma adapter
- [ ] **T2.2** Créer `src/lib/auth.ts` (config credentials provider)
- [ ] **T2.3** Créer `src/app/api/auth/[...nextauth]/route.ts`
- [ ] **T2.4** Créer `src/app/api/register/route.ts` (POST, validation Zod, hash bcrypt)
- [ ] **T2.5** Créer page `/register` avec formulaire (email, password, confirmation)
- [ ] **T2.6** Créer page `/login` avec formulaire (email, password)
- [ ] **T2.7** Configurer le middleware Next.js pour protéger `/dashboard` et `/editor`
- [ ] **T2.8** Ajouter header avec état connecté/déconnecté + bouton logout

**Gate T2** : Un utilisateur peut s'inscrire, se connecter, et être redirigé. Un utilisateur non connecté accédant à `/dashboard` est redirigé vers `/login`.

---

### Étape 3 — Éditeur Pixel Art

- [ ] **T3.1** Créer le hook `usePixelGrid` (state, paintPixel, erasePixel, undo, redo, serialize)
- [ ] **T3.2** Créer `PixelCanvas` (canvas HTML5, calcul position souris → pixel, drag to draw)
- [ ] **T3.3** Créer `ColorPalette` (DEFAULT_PALETTE, sélection couleur active)
- [ ] **T3.4** Créer `ToolBar` (crayon, gomme, undo, redo, reset, export PNG)
- [ ] **T3.5** Créer `PixelEditor` (assemblage des composants)
- [ ] **T3.6** Créer page `/editor` (nouveau dessin) — protégée par auth
- [ ] **T3.7** Créer page `/editor/[id]` (édition dessin existant) — protégée + ownership check
- [ ] **T3.8** Créer le validateur `src/lib/validators/drawing.ts` avec Zod (GridDataSchema)
- [ ] **T3.9** Créer `scripts/validate-grid.ts` (script déterministe décrit dans SKILL.md)

**Gate T3** : Le script `validate-grid.ts` retourne exit 0 sur une grille valide, exit 1 sur une grille invalide. L'éditeur permet de dessiner et d'annuler des actions.

---

### Étape 4 — API Drawings (CRUD)

- [ ] **T4.1** `POST /api/drawings` — créer un dessin (titre + gridData validés par Zod)
- [ ] **T4.2** `GET /api/drawings` — lister les dessins de l'utilisateur connecté
- [ ] **T4.3** `GET /api/drawings/[id]` — récupérer un dessin (ownership ou public)
- [ ] **T4.4** `PUT /api/drawings/[id]` — mettre à jour titre + gridData (ownership check)
- [ ] **T4.5** `DELETE /api/drawings/[id]` — supprimer un dessin (ownership check)
- [ ] **T4.6** `POST /api/drawings/[id]/publish` — publier / dépublier un dessin

**Gate T4** : Tester manuellement via curl ou client HTTP que chaque route retourne les bons status codes (200, 201, 400, 401, 403, 404).

---

### Étape 5 — Dashboard

- [ ] **T5.1** Page `/dashboard` — liste des dessins de l'utilisateur (carte avec aperçu, titre, date)
- [ ] **T5.2** Aperçu miniature du dessin (rendu canvas réduit ou SVG généré)
- [ ] **T5.3** Bouton "Nouveau dessin" → `/editor`
- [ ] **T5.4** Bouton "Éditer" → `/editor/[id]`
- [ ] **T5.5** Bouton "Publier / Dépublier" → appel API publish
- [ ] **T5.6** Bouton "Supprimer" → confirmation modale + appel API delete

**Gate T5** : Un utilisateur avec 3 dessins voit 3 cartes. La suppression retire immédiatement la carte de la liste.

---

### Étape 6 — Galerie publique & Favoris

- [ ] **T6.1** `GET /api/gallery` — lister les dessins publiés (tous les utilisateurs, paginé)
- [ ] **T6.2** `POST /api/favorites` — ajouter un favori (drawing_id)
- [ ] **T6.3** `DELETE /api/favorites/[drawingId]` — retirer un favori
- [ ] **T6.4** `GET /api/favorites` — lister ses favoris
- [ ] **T6.5** Page `/gallery` — grille de dessins publiés avec bouton favori (cœur toggle)
- [ ] **T6.6** Page `/favorites` — liste des dessins favoris de l'utilisateur connecté
- [ ] **T6.7** Indicateur visuel "déjà en favori" sur les cartes de la galerie

**Gate T6** : Un utilisateur peut ajouter et retirer un favori depuis la galerie. Le compteur de favoris se met à jour sans rechargement de page.

---

### Étape 7 — Qualité & Finalisation

- [ ] **T7.1** Configurer Jest + Testing Library
- [ ] **T7.2** Écrire tests unitaires pour `validateGridData` (5 cas : valide, dimensions hors bornes, mauvais format couleur, longueur incorrecte, dessin vide)
- [ ] **T7.3** Écrire tests unitaires pour `usePixelGrid` (paintPixel, erasePixel, undo, redo)
- [ ] **T7.4** Configurer ESLint + Prettier (vérifier `npm run lint` passe)
- [ ] **T7.5** Rédiger `README.md` (description, setup, commandes, captures d'écran)
- [ ] **T7.6** Audit de dette technique (fichier `docs/tech-debt.md`)
- [ ] **T7.7** Analyse critique collaboration humain/IA (fichier `docs/ai-collaboration.md`)

**Gate T7** : `npm run lint` → 0 erreur. `npm run test` → tous les tests passent.

---

## Schéma Prisma cible

```prisma
model User {
  id        String   @id @default(cuid())
  email     String   @unique
  password  String
  name      String?
  createdAt DateTime @default(now())

  drawings  Drawing[]
  favorites Favorite[]
  sessions  Session[]
  accounts  Account[]
}

model Drawing {
  id          String   @id @default(cuid())
  title       String
  gridData    String   // JSON stringifié (GridData)
  isPublished Boolean  @default(false)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  authorId  String
  author    User       @relation(fields: [authorId], references: [id], onDelete: Cascade)
  favorites Favorite[]
}

model Favorite {
  id        String   @id @default(cuid())
  createdAt DateTime @default(now())

  userId    String
  user      User    @relation(fields: [userId], references: [id], onDelete: Cascade)
  drawingId String
  drawing   Drawing @relation(fields: [drawingId], references: [id], onDelete: Cascade)

  @@unique([userId, drawingId])
}

// Tables NextAuth (gérées par @auth/prisma-adapter)
model Session { ... }
model Account { ... }
model VerificationToken { ... }
```

---

## Critères d'acceptation globaux (Definition of Done v1)

| Critère | Vérifié |
|---|---|
| Inscription et connexion fonctionnelles | ☐ |
| Éditeur : dessiner, effacer, undo, redo | ☐ |
| Sauvegarde et rechargement d'un dessin | ☐ |
| Publication et apparition dans la galerie | ☐ |
| Ajout / retrait de favoris | ☐ |
| `npm run lint` → 0 erreur | ☐ |
| `npm run test` → tous les tests passent | ☐ |
| Script `validate-grid.ts` → exit 0 / exit 1 déterministe | ☐ |
| README lisible par quelqu'un d'externe | ☐ |

---

## Validation humaine requise

**Avant de commencer l'implémentation**, confirmer :

- [ ] Le schéma Prisma ci-dessus est approuvé
- [ ] L'ordre des étapes (1→2→3→4→5→6→7) est validé
- [ ] Les fonctionnalités OUT OF SCOPE sont acceptées pour la v1
- [ ] La stack (Next.js + SQLite + NextAuth + Zod) est confirmée

**Signature humaine** : _______________  **Date** : _______________